<Window x:Class="EmotionQuest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EmotionQuest – Podróż przez emocje"
        Width="1280" Height="1024"
        WindowStartupLocation="CenterScreen"
        Background="#FF121212">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <!-- Górny pasek HUD -->
            <RowDefinition Height="*"/>
            <!-- Środkowy bodziec -->
            <RowDefinition Height="160"/>
            <!-- Dolne przyciski emocji -->
        </Grid.RowDefinitions>

        <!-- ===========================
             GÓRNY PASEK (HUD)
        ============================ -->
        <DockPanel Grid.Row="0">
            <!-- Lewa część: tytuł gry -->
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="EmotionQuest"
                           FontSize="28"
                           Foreground="White"
                           FontWeight="Bold"/>
                <TextBlock Text="Podróż przez emocje"
                           FontSize="16"
                           Foreground="#FFCCCCCC"/>
            </StackPanel>

            <!-- Środkowa część: wynik i poziom -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0,0,0,0">

                <TextBlock Text="Wynik: "
                           FontSize="18"
                           Foreground="#FFCCCCCC"/>
                <TextBlock x:Name="ScoreText"
                           Text="0"
                           FontSize="18"
                           Foreground="White"
                           Margin="0,0,16,0"/>

                <TextBlock Text="Poziom: "
                           FontSize="18"
                           Foreground="#FFCCCCCC"/>
                <TextBlock x:Name="LevelText"
                           Text="Łatwy"
                           FontSize="18"
                           Foreground="White"/>
            </StackPanel>

            <!-- Prawa część: ikony (ustawienia, zapis, restart) -->
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right"
                        VerticalAlignment="Center">

                <Button x:Name="SettingsButton"
                        Content="⚙"
                        FontSize="20"
                        Width="44" Height="44"
                        Margin="4"
                        Background="#FF2D2D2D"
                        Foreground="White"
                        BorderBrush="#FF444444"/>

                <Button x:Name="SaveButton"
                        Content="💾"
                        FontSize="20"
                        Width="44" Height="44"
                        Margin="4"
                        Background="#FF2D2D2D"
                        Foreground="White"
                        BorderBrush="#FF444444"/>

                <Button x:Name="RestartButton"
                        Content="🏁"
                        FontSize="20"
                        Width="44" Height="44"
                        Margin="4"
                        Background="#FF2D2D2D"
                        Foreground="White"
                        BorderBrush="#FF444444"/>
            </StackPanel>
        </DockPanel>


        <!-- ===========================
             ŚRODEK: bodziec
        ============================ -->
        <Border Grid.Row="1"
        Margin="0,16"
        BorderBrush="#FF3A3A3A"
        BorderThickness="2"
        CornerRadius="16">

            <!-- Tło obrazkowe tylko dla tego Border -->
            <Border.Background>
                <ImageBrush ImageSource="Assets/background-1524540_1280.jpg"
                    Stretch="UniformToFill"/>
            </Border.Background>

            <!-- Przyciemnienie + tekst na wierzchu -->
            <Grid Margin="24" Background="#80000000">
                <TextBlock x:Name="StimulusText"
                   Text="Tutaj wyświetli się opis sytuacji emocjonalnej."
                   TextWrapping="Wrap"
                   FontSize="24"
                   Foreground="White"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   MaxWidth="900"/>
            </Grid>
        </Border>


        <!-- ===========================
             DÓŁ: przyciski emocji
        ============================ -->
        <Border Grid.Row="2"
                Margin="0,16,0,0"
                CornerRadius="16"
                Background="#FF1A1A1A"
                BorderBrush="#FF3A3A3A"
                BorderThickness="2">

            <UniformGrid Columns="5" Margin="16">

                <Button x:Name="EmotionJoyButton"
                        Content="Radość 😀"
                        FontSize="20"
                        Margin="8"
                        Padding="8"
                        Background="Yellow"
                        Foreground="Black"/>

                <Button x:Name="EmotionSadnessButton"
                        Content="Smutek 😢"
                        FontSize="20"
                        Margin="8"
                        Padding="8"
                        Background="#FF5C6BC0"
                        Foreground="Black"/>

                <Button x:Name="EmotionAngerButton"
                        Content="Złość 😡"
                        FontSize="20"
                        Margin="8"
                        Padding="8"
                        Background="Red"
                        Foreground="Black"/>

                <Button x:Name="EmotionFearButton"
                        Content="Strach 😱"
                        FontSize="20"
                        Margin="8"
                        Padding="8"
                        Background="Purple"
                        Foreground="Black"/>

                <Button x:Name="EmotionCalmButton"
                        Content="Spokój 😌"
                        FontSize="20"
                        Margin="8"
                        Padding="8"
                        Background="Green"
                        Foreground="Black"/>

            </UniformGrid>
        </Border>

    </Grid>
</Window>

